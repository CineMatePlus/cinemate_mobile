# Current release notes

Older academic descriptions are not the source of truth for implemented features. The current [architecture](architecture.md), root README and backend OpenAPI define the supported scope.

The portfolio work updates the Android toolchain, migrates model generation, aligns refresh sessions with the backend, removes credential logs, handles network failures, fixes private collections and recommendation pagination, and adds automated tests and real application screenshots.

Unverified platforms and future features are explicitly listed in the architecture document. Final test evidence and remaining external publication steps are recorded in the backend `docs/completion-status.md`.
