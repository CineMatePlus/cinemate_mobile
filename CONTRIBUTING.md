# Contributing

Use the pinned SDKs documented in README. Include a behavior test for auth, API-contract and user-data changes. Run code generation, formatting, analyze, tests and normal Android build before proposing a change.

Use synthetic accounts. Do not commit local API credentials or signing material. If an API contract changes, update `.backend-ref`, mobile models and the real-backend integration test together.
